// Logical "&&"" Operator
let rain = true;
let wind = true;
if (rain && wind) {
	console.log("Really stay at home!");
}

// Logical "||" Operator
let day = "Saturday";
if (day === "Saturday" || day === "Sunday") {
	console.log("It's the week-end!");
}

